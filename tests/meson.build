
if get_option('build_tests')
    #
    # Import dependencies.
    #
    gtest_main_dep = subproject('gtest').get_variable('gtest_main_dep')

    subdir('Common')
    subdir('DLL')
    subdir('Plugin')
endif
